using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class logic_script_menu : MonoBehaviour
{
    public GameObject Script_Client;

    public GameObject player_ship;
    public GameObject player_ship_object;
    public GameObject planete_ice;


    // Start is called before the first frame update
    void Start()
    {
        //player_ship = Instantiate(player_ship_object);
        //Script_Client.GetComponent<WebSocketClient_menu>().setScript(player_ship.GetComponent<menu_move_player>());
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
